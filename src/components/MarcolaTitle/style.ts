import styled from 'styled-components'

export const MarcolaTitleStyle = styled.div`
    h1 {
            display: inline-block;
            position: absolute;
            font-family: 'Nunito', sans-serif;
            font-weight: 800;
            font-size: 30px; 
            margin-top: -50px; 
            margin-left: 90px;
        }
`
