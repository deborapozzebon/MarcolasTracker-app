import styled from 'styled-components';

export const ButtonConfirmStyle = styled.div`
    position: absolute;
    padding: 10px;

    button {
        position: absolute;
        width: 548px;
        height: 64px; 
        background: #37C77F;
        border-radius: 20px;
        font: 600 18px 'Nunito', sans-serif;
        color: #FFF;
        border: none;
        cursor: pointer;

        &:hover {
            background: #3EE08F;
        }
    }

`