import styled from 'styled-components';

export const FormLabelDownStyle = styled.div`
    padding: 0 10px; 
    position: relative; 

    label {
        font: 600 16px 'Nunito', sans-serif;
        color: #8FA7B2;
        position: relative;
        margin: 24px 0 24px 0;
    }

    input {
        padding-top: -30px;
        width: 548px;
        height: 100px; 
        background: #D3E2FA;
        border: 1px solid #D3E2E5;
        box-sizing: border-box;
        border-radius: 20px;
        font: 600 16px 'Nunito', sans-serif;
        color: #5C8599;
        float: inline-start;
    }
`